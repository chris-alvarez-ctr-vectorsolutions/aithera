# Scenario Editor — stable and sandbox

Two builds of one tool.

| | URL | What it is |
| --- | --- | --- |
| **Stable** | `scenario-editor/` | The editor LXDs rely on to create and edit real scenarios. The dev team builds their own authoring environment against this one. |
| **Sandbox** | `scenario-editor/?v=2` | Quick UI and flow experiments, for testing ideas with the LXD team. Nothing here is a commitment. |
| **Frozen integration cut** | `scenario-editor/integration/` | What Learning Studio iframes. A self-contained copy that does not move when the others do — see its own `README.md`. |

The bare folder URL is always stable — that is the whole reason the editor moved
into a folder. `?v=2` hands off to the sandbox and forwards every other
parameter, so a shared `?type=…&example=…` link survives the hop. The old
`writer-studio-v2.html` path still redirects, because links to it are already out
in Slack, Jira and the docs.

## What isolation you actually get

**Drafts and libraries are separate.** `localStorage` is per origin, not per
page, so without this the sandbox and the stable editor would share every key —
and since the browser library is the only copy an authored scenario has, a
half-finished draft written by an experiment would land on someone's real work.
The sandbox declares `window.STUDIO_CHANNEL = 'sandbox'` before the engine
loads and gets its own draft and library keys. Stable keeps the original keys
untouched, so nobody lost a library when the sandbox appeared.

The publish→live buffer stays **shared** on purpose: it is how a playtest reaches
the player, and channelizing it would mean a sandbox playtest publishing
somewhere no player looks. A sandbox publish costs a re-publish. A sandbox
library write would have cost the work.

**Code is not automatically separate**, and this is the part worth internalising:
the editor page is a thin shell over about twenty modules in `../js/`, and most
of them are shared with the player. So:

- An experiment that only touches files under `scenario-editor/sandbox/` is
  **free** — change whatever you like.
- An experiment that needs a shared module (`studio-shell.js`, a wizard, a
  scenario type, `scenario-v4.js`) **changes stable too**. To keep it in the
  sandbox, copy that one file into `scenario-editor/sandbox/js/` and point the
  sandbox page at it — note that because the page carries `<base href="../../">`,
  a forked copy is referenced as `scenario-editor/sandbox/js/<file>.js`, since
  plain `js/<file>.js` would load the shared one.

That line is not bureaucracy — it is exactly the boundary the dev team asked
for. Anything that reaches stable is something they may have to build.

## Promoting a change to stable

Stable is a **place, not a version number**: promoting means the change lands in
`scenario-editor/index.html` and its modules, so the stable URL never moves.
Versions accumulate here as notes, not as folders.

Before anything is promoted, it gets a conversation with the dev team so it can
go on their roadmap. Two things make that conversation short:

1. **Say whether the JSON contract moved.** Not an opinion — run the check:
   `node tools/roundtrip-check.js`. It replays the real export chain over the
   dev team's live scenario documents and fails on any difference. A change that
   keeps it green is a UI change; one that turns it red is a contract change and
   needs to be discussed as one.

   Run `node tools/surface-check.js` too. It answers the question the contract
   check structurally cannot — *do the pages still point at the files they claim
   to?* — and it is the one to run after moving or renaming anything, because
   that is when a script tag gets left behind. Both run in CI on every push.
2. **Say what prompted it.** The sandbox exists to gather LXD input for an
   authoring environment the dev team will eventually build themselves, so the
   durable output is the evidence, not the diff.

## Log

Newest first. `Contract` = what `tools/roundtrip-check.js` said.

| Date | Build | Change | Why | Contract | State |
| --- | --- | --- | --- | --- | --- |
| 2026-09-03 | integration | **Re-cut to bring the K&A build current** — `v4-universal.js` and `writer-studio.css` refreshed, stamps 26→27 and 24→25. All 16 files now match stable exactly | The cut had fallen two files behind after the previous re-cut: the id read-out trim (`19014671`) landed just after that run, plus its stamp move (`41377704`). Nothing was wrong with the cut — it lags by design, moving only on a deliberate re-cut — but the gap was the id work arriving in K&A's build in two halves, the generated ids without the copy trim that makes them readable. So this closes it rather than leaving an author looking at the same provenance sentence repeated on all four id rows. Verified the script captured COMMITTED state: all 16 copied files match HEAD byte-for-byte, which matters because it reads from disk and this tree has several sessions writing to it | green (11/11) | shipped |
| 2026-09-03 | integration | **Re-cut: the drop zone leaves the K&A build, and the reseeded templates arrive.** All 16 files refreshed from stable; stamps moved FORWARD — `writer-studio.css` 23→24, `v4-universal.js` 25→26 (templates v=5 and studio-shell v=37 were already current) | The cut was the last place the drop zone still lived: `5dd2daa0` put it there and it had been SERVED under v=25, so it could not be excised by hand — swapping content under a stamp browsers already hold is the failure that actually reaches people. A re-cut is the correct instrument because it refreshes and re-stamps in one move. The exhibit field is a plain path box again, carrying the reseeded templates' real paths. On the known media gap: `recut-integration.js` follows src/href out of index.html, so the three images at `assets/media/` — referenced by scenario DOCUMENTS, not by the page — do not travel into the cut. That is now inert rather than visible: with the thumbnail reverted, nothing in this build loads an exhibit image, so the path is plain text in a field and is resolved downstream by Learning Studio. Verified: no failed request, nothing leaving `/integration/`, `STUDIO_CHANNEL` still 'integration' | green (11/11) | shipped |
| 2026-09-03 | both | **The exhibit drop zone was built, shipped and rolled back the same day** — the field is a plain path box again. Recorded rather than deleted, so it is not rebuilt from scratch on a hunch | It was built to answer "there is nowhere to set the Observe image", and that reading of the step was wrong: an Observe step is a RUBRIC — a list of findable items — and the exhibit is the backdrop, not the thing being authored. The request came from conflating it with the retired scene-sweep hit-target editor, which is a genuinely different feature (drawing polygons over a photo, removed with its scenario type in the 2026-08-24 relocation and never ported to v4, whose rubric carries no geometry). Worth revisiting only alongside that question, not on its own. The reader is still recoverable from the reverted commit and from scene-sweep.js. NOTE: the frozen cut still carries the drop zone until its next re-cut — `5dd2daa0` shipped it there and `integration/index.html` was mid-edit by another session when this was reverted, so pulling it needs a forward stamp bump rather than a content swap under the already-served `v=25`. The three fixes from `6d7669d3` are KEPT: they are bugs regardless of how the step is authored | green (11/11) | reverted |
| 2026-09-03 | both + integration | **A template now seeds the locked production document, not a port of our exemplar** — all six gallery entries reseeded from `tools/pinned/content` (Guided Arc ← marshall, Escalating Situation ← wpv, Ensemble ← bullying, Scene Sweep ← hazcom, Mix & Match ← floor-lead, Observe & React ← dock-walk) by the new `tools/reseed-templates.js`, which adds only a readable `template-…` implementation_id (never their production UUID — an export made from one would address the real record), prior-scenario context, and an `exhibit.src` repointed at an image vendored into `assets/media/`. Each pick now lands 0 required fields short and would load in the production engine as it stands. The gallery also names the scenario each row loads, and **both screens now count the same document**: `templateFacts()` measures the template on gallery open and the read-out measures what landed, so the stored `toFill` is gone | The templates were ports, and conversion policy D6 forbids inventing prose, so each one arrived with 8-24 required fields blank — an LXD promised the Marshall scenario got a form with red rows in it. The dev team's own documents were already in this repo for the round-trip check, six of them matching these six shapes exactly, so the honest fix was to stop porting. The number was its own bug: the card read `~24 fields to fill` from data written against the un-normalized port while the read-out counted the real thing and said `9 flagged` — same click, 2.6x apart, no way to tell which was true. Neither number is stored now. What remains blank after a pick is optional apparatus the production document does not use (the debrief's conditional probe, the opening's deliberately partial tiers, handoff lines) — pages Start, Situation & World and Teaching land with nothing blank at all | green (11/11, field coverage green; 5 pinned documents report upstream DRIFT — titles have moved to catalog names upstream, refreshing the pins is its own deliberate step) | shipped |
| 2026-09-03 | both | **Every id is generated now — the five id fields are gone.** The implementation id follows the scenario title; a character id, a step id and a rubric item's crediting key follow the name beside them; the opening's is a constant. Each one shows as a read-out under the field it derives from rather than as an input. **Renames cascade**: rename a character and the steps that speak to them follow, rename a step and every carryover reading from it follows. The three fields that *reference* an id — the roleplay counterpart, an opening line's speaker, and a carryover's source step — are pickers over names instead of free-text boxes | Not one of the five was a decision an author makes, and all five were text boxes: five chances to typo a key another field silently references, in exchange for nothing. The three reference fields were worse — free text whose only valid values lived on a different screen, and with the id fields gone there would have been nowhere left to read them off. What makes removing them safe rather than merely tidy is the cascade plus one rule: an id that ARRIVED with a document is frozen, forever. Provenance is recorded per record in `__auto_id` (stripped by `prune`, so it never reaches the validator, the export or the player), and only a record the editor generated is ever re-derived — which is why eleven production documents still round-trip byte-for-byte. A template hands back only its *scenario* id; its step and character ids stay frozen, because its carryover graph is already wired against them. An unnamed row deliberately gets NO id, so a half-added character still drops at prune instead of becoming a validation error about a character nobody meant to declare. Field coverage keeps passing via the IMPERATIVE list in `roundtrip-check.js` — the same escape hatch `numField` and `enumField` already use, since a static scan for `tf()` literals cannot see a generated value. Verified in the browser: derivation, live read-outs, both rename cascades, collision suffixes (`sam` / `sam-2` beside a frozen `sam-3`), and an imported UUID surviving a load untouched | green (11/11) | shipped |
| 2026-09-03 | integration | **Re-cut the frozen build** — `studio-shell.js`, `writer-studio.css` and `scenario-v4-runtime.js` refreshed from stable, and the cut's three `?v=` stamps moved to match. Nothing else had drifted | The cut is only worth anything if it moves deliberately, so this records what was let in and why. Two things: the paced template hand-off, which K&A are the people who hit the *nothing happened* reading in the first place; and the compiler carrying `exhibit.type`, without which the exhibit-kind dropdown in their build still decides nothing. The observe-stage empty state is NOT in here — it lives in the player, which the cut deliberately does not copy, so it reaches K&A only when they preview against our player rather than from inside Studio. Re-verified self-containment after copying: 16 local refs, all resolving inside the folder, none escaping, and a load shows no same-origin request leaving `/integration/` | green (11/11) | shipped |
| 2026-09-03 | both | **An authored image exhibit renders as an image, and an Observe step with no exhibit says so.** Three fixes on one path: `mediaFromInteraction` carries `exhibit.type` into the media segment as `mediaType`; the clip card picks `<img>` or `<video>` from it, sniffing the extension (and `data:image/`) when a producer sets none; and the text-observation stage renders a stand-in — the authored description plus which failure it is — instead of `<img src="">`. Not applied to the integration cut | The exhibit-kind dropdown decided nothing: the compiler dropped `exhibit.type`, and the clip card renders a `<video>` for any src it is handed, so an authored photo arrived as a black box with playback controls. Underneath it, the observe stage had no empty state at all — a step with no image source, a path that 404s, and an image still loading were the same blank rectangle, which is how this surfaced: a scenario previewed to a blank frame with nothing naming the cause. The stand-in shows the description because it is authored regardless — it is the same ground truth the coach reasons over, so the sweep still works from the text. Sniffing keeps the older segment producers safe without asking them to declare anything, and an unrecognised src stays video, which is what every pre-existing clip is. Verified: 9/9 renderer cases, both compiled templates carry the right `mediaType`, and all three stage states render | green (11/11) | shipped |
| 2026-09-02 | both | **Picking a template hands over through a paced read-out** instead of closing the panel. The pick stays in the modal, ticks three lines as each real step runs (read the template · fill every field · check what still needs words), then reports what landed — values filled in, how many the validator flagged, editable sections, and whether the previous draft was snapshotted — behind a *Start editing* button. Not applied to the integration cut | An LXD picking Scene Sweep read the transition as *nothing happened*, and clicked the same row again. Two reasons, and neither is fixed by a better toast: a template-filled form and a half-filled one are the same three columns of fields at a glance, and the author is looking at the modal — which vanished — not at the strip that replaced it. So the panel says what it did while it does it. The pacing is real work, not a spinner: each line runs its own step and the numbers are counted out of the document that actually landed, which is also why they read higher than the template's own `toFill` (normalize adds fields). Escape mid-sequence still completes the pick — closing early skips the read-out, it does not cancel what was asked for | green (11/11) | shipped |
| 2026-09-02 | both | **Three ways to reach the two starting flows without the top bar**: `?new=1` and `?wizard=1`, a `window.AitheraStudioHost` handle, and a `postMessage` listener accepting `{ source: 'learning-studio', action: 'new-scenario' \| 'wizard' }` from the embedder only. Re-cut into the integration build | Hiding the chrome removed the only route to the template gallery, the AI draft and open-an-existing-scenario. The three exist because they are not interchangeable: a parameter needs a reload, which suits Learning Studio opening a fresh iframe after creating the Learning Object, but throws away the author's work if used for "start over"; the window handle cannot be called cross-origin, which is what Learning Studio is; so postMessage is the only one that works from the real embedder. Verified against a fake wrapper page: the panel opened and the acknowledgement came back. Scoped to these two actions on purpose — a replacement for one hidden button, not a proposed event contract for save, publish and preview | green (11/11) | shipped |
| 2026-09-02 | integration | **A frozen, self-contained cut at `scenario-editor/integration/`** — its own copy of all 16 modules and the stylesheet, embedded by default, `STUDIO_CHANNEL = 'integration'`, channel router removed | Learning Studio is iframing the editor so K&A can edit existing scenarios from Studio, and that authoring flow must not move while we keep iterating. The 2026-08-20 stable/sandbox split isolated *drafts*, not code — `sandbox/js/` is empty and every module is shared — so iterating anywhere in `js/` would have reached the build K&A are working in. A self-contained cut is the only thing that actually decouples them. Verified by appending a marker to the live `studio-shell.js`: the live editor picked it up, the cut did not | green (11/11) | shipped |
| 2026-09-01 | stable | **`?embed=1` hides both chrome bars** — the top bar (Local drafts · New scenario · Export JSON) and the published-state strip (Preview as learner · clear draft). Handlers and elements are untouched | Learning Studio is integrating the editor in an iframe and driving Save, Publish and Preview from its own window, so the editor's bars would offer a second, competing set of the same actions. Opt-in by parameter and NOT by detecting a frame: this repo's dashboards and version loader render pages in frames, and the standalone editor — the one heading for customer-facing delivery — keeps its chrome wherever it is shown. The class is set by an inline script before paint so the bars never appear and then vanish. Nothing is disabled or removed: the wrapper may want to drive the existing export and preview paths from its own controls, and a dozen call sites read these elements by id | green (11/11) | shipped |
| 2026-08-26 | stable | **Both export buttons are named for what the file is, not what it is called**: *Download the editor file* and *Download the player file* | The working-draft button said `Download scenario.json` long after the filename stopped being that — it now hands over `<scenario>-draft-<date-time>.json`, so the button named a file that no longer exists. The dev-handoff button had the same shape of problem in reverse: `.lo.json` is an extension, not a description, and an LXD reading the two buttons could not tell which one Learning Studio wants back. The Studio integration guide names them the editor file and the player file, so the buttons now say the same words the doc does | green (11/11) | shipped |
| 2026-08-25 | stable | **The three content-safety toggles are gone**, along with their extension declarations, runtime arming, wizard questions and template values | Dev confirmed the production engine carries always-on safety guidance in its general prompt and that authors do not control what is enabled — so the toggles offered a decision nobody gets to make, armed nothing downstream (the export stripped them), and made every export report a loss for no gain. The UNCONDITIONAL learner-safety section still compiles on every scenario; only the three per-scenario topic floors went. Safety is a V1 product feature, not a preview feature | green (11/11) | shipped |
| 2026-08-25 | stable | The export panel's `answer_shape` consequence said the opposite of what happens | It read "stripped, every practice reads as determinate". The runtime does `answer_shape === 'determinate'`, so an absent marker is OPEN — deliberately, as the safe default. An author weighing whether the strip mattered was told the stakes backwards | green (11/11) | shipped |
| 2026-08-25 | both | **A consistency sweep, and a check so the same drift cannot come back.** `tools/surface-check.js` now fails the build on a dead local `src`/`href` or on one file loaded at two different `?v=`. It found: a script tag in `layered-course-scenario.html` still pointing at `js/scene-context.js` after the simulator got its own directory (so that page's context-setter had been silently absent); `doc-view.html` left behind in `lesson-presentation/` with all four of its documents and all three footer links 404; eight moved/renamed composed-prompt dumps; two docs reaching for the runtime schema at its old path; and six shared modules pinned at stale versions across the editor, the player and the lesson pages. All fixed. Also: six wizard/scenario modules nothing loads were deleted (204KB) — the reason recorded for keeping them, that the archived pages needed them, was wrong, since each archive cut carries its own `js/`. And the plan page's positions 02 and 05 were marked shipped with the numbers the code actually has | Every one of these was found by reading, in a sweep, after the fact — which is the failure mode, not the bugs. A path that resolves and a version that agrees are not judgement calls, so they should not need a person to remember to look. The two that mattered were invisible by construction: a 404 script tag renders a page that merely does less, and a stale `?v=` looks exactly like a fix that did not work | green (11/11) | shipped |
| 2026-08-25 | stable | **Every leaf a production scenario carries now has a field.** Coverage went 50 → 0. New editors: the quality levels on a **debrief**, on a **debrief's probe** and on the **opening**; a character's **guardrails** and **known facts with reveal conditions**; the opening's **id / composer placeholder / handoff line / conditional probes**; a **practice name**; a debrief's **chat header** and **handoff line**; opener **emotion**; and the **exhibit / ambient-reference kind** | The coverage check found them, and three mattered: `debrief.levels` is authored in 9 of the 11 production documents, `behavior.guardrails` in 8 (the closest thing the format has to a per-character safety constraint), and `canon_facts.reveal_when` is earned disclosure — the mechanic our own ensemble scenarios pioneered, with nowhere to author it. The opening's card had been telling authors its levels were "partial on purpose" above no levels editor at all | green (11/11) | shipped |
| 2026-08-25 | stable | The quality-levels editor is now ONE shared block (`levelsBlock`) used by all four sites, and it no longer pre-creates absent tiers | It was written once inline for a practice, which is exactly why the other three sites had none. Pre-creating `{}` for a missing tier mutates the draft just by looking at it — harmless where every document has all three, but on the opening (whose schema requires none) it would invent tiers the document never had and break an edit-free round trip | green (11/11) | shipped |
| 2026-08-25 | stable | `rowCard` renders no delete control when given no handler | The three quality levels are the engine's vocabulary, not a list an author adds to, so their cards were passing `null` — and `addEventListener('click', null)` is legal, so twelve cards showed a bin that silently did nothing | green (11/11) | shipped |
| 2026-08-25 | stable | **The step editor renders its inputs again.** A numeric field's change handler now returns when nothing actually changed | `value-changed` is a PROPERTY event, not a user event: Vaadin emits it asynchronously after upgrade, so the programmatic `f.value` set at build time arrived back in the handler after its listeners attached. "Follow-up turns" repaints the debrief on change (0 restructures it), so it rebuilt the field, whose value was set again, which fired again — twenty nested repaints, a `Maximum call stack size exceeded` from inside the component bundle, and every field built before it marked failed. On screen: a step card with no inputs at all, 138 fields where 38 belong. It never fired while the tag was `vaadin-integer-field`, which the library does not register — making it a real field woke the loop up. A dead control can hide a live bug | green (11/11) | shipped |
| 2026-08-25 | both | Components bumped to **core v1.22.3 / themes v1.9.3** | The editor was three minors behind the component library the dev team will build against, and behind this repo's own base template. Verified: 16/16 fields upgrade on Situation & World, nothing regressed | green (11/11) | shipped |
| 2026-08-25 | stable | Numeric inputs use `vaadin-number-field` with `theme="outlined"` | They used `vaadin-integer-field`, which the Vector core bundle does not register at ANY version and which is absent from the CONTEXT element list — so five inputs (practice turn budget, debrief follow-up turns, both help budgets, spot target) were undefined custom elements: 0px, no shadow root, label and value living as properties on an inert node. `practice.exit.when.turns` is required, so Validation demanded a field with no way to fill it. Silent, because an unknown tag is not an error | green (11/11) | shipped |
| 2026-08-25 | stable | `answer_shape` is written **only when determinate** — 16 redundant `"open"` markers dropped from the templates, and the toggle/wizard/`newPhase` no longer record it | Absent already reads as open (`scenario-v4-runtime` tests `=== 'determinate'`), so recording 'open' declared an extension, earned a stripped-extension warning and made the export lossy — all to say what saying nothing says. The 2 genuine `determinate` markers stay, and still warn, because there the field carries meaning | green (11/11) | shipped |
| 2026-08-25 | both | The compiled-prompt legend stops claiming *"this exact text is what the AI runs on"* | True of the preview, false of production: this prompt is assembled by our compiler and the production service assembles its own from the same document. Two builders, and neither side would notice them drifting. Not a fix — it stops the tool asserting something untrue | green (11/11) | shipped |
| 2026-08-25 | stable | Every download is named `<scenario>-[draft-]<date-time>` by one shared scheme (`exportName`), and the export copy says the LMS owns versioning rather than claiming the file stem is the scenario id | The working draft was called `scenario.json` — every scenario, every time — so two exports landed in Downloads as `scenario.json` and `scenario (1).json` with nothing on either saying which scenario it held or which was newer, and a re-export after an edit silently replaced the previous one. The scenario's identity travels inside the document in `implementation_id`, so the filename is free to be for people | green (11/11) | shipped |
| 2026-08-25 | stable | Template gallery down to six: **Branching Arc renamed Escalating Situation**, **Teach Back retired** (object kept, out of `ORDER`) | Neither was redundant — both promised a mechanic Scenario CML v4 cannot express. v4 has no branching construct at all (no `branch`/`next`/`condition`/`goto`/`target` in the schema, `transition` is a button label, a phase has no successor), so "Branching Arc" emitted a straight `C→R→R→R` ladder; "Teach Back" emitted one `coach_inquiry` step under a blurb promising credited coverage, which only `observe_react` has. `observe-react` is also one step and stays, because its blurb describes what it produces — the test is honesty, not step count | green (11/11) | shipped |
| 2026-08-25 | stable | The **Guardrails** inspector tab is now **Validation**; the rail keeps *System guardrails* for the locked prompt sections | Two different things wore one word on the same screen. "Validation" is also what the production service's authoring API calls the same output (`failures[]`, and its own inspector tab), so both tools now name it the same | green (11/11) | shipped |
| 2026-08-25 | stable | Validation rows name the field the author sees, repeated rows collapse into one that lists the steps, and the per-row "POC V4 rejects the document" refrain is gone | Twelve rows carried raw schema paths (`practice.interaction.opening_messages`) and the same sentence underneath each, with six of them being two messages said three times. The panel's job is telling an LXD which field to fill | green (11/11) | shipped |
| 2026-08-25 | stable | Guided Arc's vocabulary is out of the rail: the **Learn** step is **Teaching**, the dead **Practice** step is deleted, and Interaction/Voice & Tone lose their engine-jargon ledes | The Learn step held nothing but the teaching points — which the card on it calls debrief-scoped and never shown mid-attempt — under a heading promising a gut-reaction warm-up and topic turns. Guided Arc has not been authored here since it left the load list | green (11/11) | shipped |
| 2026-08-25 | stable | **Nested lists are editable.** A teaching topic's points and an expert-answer group's components were each bound to index 0 — one field, first entry, rest hidden. Both are repeaters now (`subRows` in the shell) | Every one of the 36 teaching topics in the eleven production documents carries more than one point (up to 10), and 24 of 37 component groups carry more than one component: 152 authored fields an LXD could not see or edit. The hidden entries survived export, so nothing failed — `roundtrip-check.js` stayed green while the editor was showing a quarter of the document | green (11/11) | shipped |
| 2026-08-25 | stable | The dev-handoff export reports the prior-scenario context it drops, and the lints flag it at authoring time | The shell keys were stripped BEFORE `stripExtensions` ran, so the removal never entered the report — and the panel then said "Nothing else is changed" while the one thing the 2026-08-18 meeting approved went out with the rubbish | green (11/11) | shipped |
| 2026-08-25 | stable | First visit opens the New scenario panel instead of an empty form, and no longer parks a phantom "(untitled)" snapshot in Local drafts | `JSON.parse(null)` returns null without throwing, so the boot's `catch → DEFAULT` fallback was dead code and a first-time author got a blank three-column form with a red dot on every section. The pristine check then measured that blank against DEFAULT alone, counted it as work, and snapshotted it | green (11/11) | shipped |
| 2026-08-24 | stable | Every `window.confirm()` replaced with an in-page dialog — deleting and opening a local draft, clearing the draft from the player, and three wizard guards. Snapshots also list under their real name instead of "(untitled)" | A native dialog is suppressed in an embedded browser: it returns false without ever prompting, so delete/open did nothing and the wizard guards refused to let go. `listLibrary` read `scenario.title`, which POC V4 keeps under `content` | green (11/11) | shipped |
| 2026-08-24 | stable | The Steps card stops repeating the rail: the step list is replaced by the arc as one line plus the derived conversation cap | The rail already lists the steps, on the same screen — the same list twice is a second thing to keep in sync. The cap (every turn budget added up) is the one thing the rail cannot show | green (11/11) | shipped |
| 2026-08-24 | stable | Delete asks in the menu, not with `window.confirm()`; the rail widened to 292px; an open ⋯ menu no longer paints under the rows below it, and no longer vanishes when the debounced save repaints the rail | A native dialog is suppressed in an embedded browser — it returns false without ever prompting, which made Delete a button that silently did nothing | green (11/11) | shipped |
| 2026-08-24 | stable | **The steps are their own rail entries.** Each step opens alone in the editing pane; the Steps card became an arc overview; drag or the per-step ⋯ menu moves, duplicates and deletes them; deleting prunes carryover references and a reorder that breaks one says so | Four steps stacked as accordions in one card meant editing step 4 was a scroll past the other three, and the arc had no reorder at all. The rail is where an ordered spine belongs | green (11/11) | shipped |
| 2026-08-20 | stable | Context is never set in-scenario: the choice is gone, the Start step just asks what ran before (coach-facing only) | The product will not set learner context inside a scenario — the surrounding learning object owns the run-up | green (11/11) | shipped |
| 2026-08-20 | stable | The world step no longer disappears with inherited context, and is retitled **Situation & World** | Latent defect: that step also carries setting, canon and characters, so "inherited" made the scene world unauthorable — and "always inherited" would have hidden it for good | green (11/11) | shipped |
| 2026-08-20 | stable | `help_turns` compiles to 2 when absent, matching the production loader (was `null`) | The two engines disagreed about an absent field. Note the affordance itself is not built in our player — this aligns the number, not the behaviour | green (11/11) | shipped |
| 2026-08-20 | both | Header names the build: **Current** vs **Prototype**, with a notice in the prototype saying its changes are not in the everyday editor until released | An author had no way to tell which build they were in, and the old "V2" tag collided with `?v=2` meaning "prototype" | green (11/11) | shipped |
| 2026-08-20 | stable | Editor moved to `scenario-editor/`; sandbox build added at `?v=2` with isolated drafts | Stable had to be the tool of record with a URL that never moves, and an experimental build must not be able to overwrite an author's only copy of a scenario | green (11/11) | shipped |
| 2026-08-20 | stable | An edit-free export no longer drops `closing.ideal_response.source_references` | Six of the dev team's eleven live documents lost the field on a round trip; their schema permits an empty array there and ours deleted it | green (11/11), was 5/11 | shipped |
