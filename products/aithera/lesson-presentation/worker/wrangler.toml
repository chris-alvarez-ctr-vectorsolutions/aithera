# Cloudflare Worker config — only needed if you deploy via the wrangler CLI.
# (The dashboard copy-paste path in README.md doesn't use this file.)
name = "aithera-action-proxy"
main = "worker.js"
compatibility_date = "2026-07-01"
