{
  "version": 1,
  "_comment": "Maintained by Claude — see ../CLAUDE.md 'Dashboard Maintenance' rule. Status values: concept | in-progress | review | ready | archived. Keys in 'mocks' are folder paths relative to products/Scheduling/ (no trailing /index.html).",
  "jiraBaseUrl": "https://lmsportal.atlassian.net/browse/",
  "recentChanges": [
    {
      "date": "2026-06-23",
      "path": "deployment/index.html",
      "summary": "Laid everything out (less progressive disclosure): V3 now shows all three parts — dates, assignments, details — at once (numbered cards, no collapse-to-strip) alongside the sticky side review rail. And selecting the 'Create deployment' toggle now surfaces the full position builder INLINE (no 'Create new template / shell' click first) and lays out the build + details immediately in V1/V2 too, since create-from-scratch has no date dependency. Existing mode keeps its dates-first gating; deploy-enable uses the date values directly in create/V3."
    },
    {
      "date": "2026-06-23",
      "path": "deployment/index.html",
      "summary": "Fleshed out the 'Create deployment' build path across V1/V2/V3: 'Create new template / shell' now builds qualifier-tagged positions (click FF / Capt / Eng / AO / BC, a shared qual PM/EMT/HM, or 'Open' to add each position) instead of just a position count — mirroring the Figma template builder. Each position becomes an open slot to fill now or later; created shells show as selectable cards with qualifier badges and count toward the deployment."
    },
    {
      "date": "2026-06-23",
      "path": "deployment/index.html",
      "summary": "V3 wizard: replaced the inline running-summary line with a sticky side review rail (per design choice). The rail shows the mode (Create / Select existing), per-step state with check marks (Dates, Assignments, Templates, Details) and live counts, plus Deploy + Cancel — so as each step collapses the summary and the Deploy action stay in view without scrolling."
    },
    {
      "date": "2026-06-23",
      "path": "deployment/index.html",
      "summary": "Reworked all three deployment design versions to one unified model: a 'Create deployment / Select existing assignment' toggle + the SAME shared details (name, code, location, dates, on/off-duty work types, notes), a build area that handles a single one or many with no separate single/multiple step, and template-shell creation in either mode. The versions now differ only in dates presentation: V1 puts dates in an inline card that collapses, then build then details; V2 puts dates in a popup modal (Edit dates reopens it), then build then details; V3 is a 1-2-3 step wizard (dates → assignments+template → details) where each step collapses to a one-line summary with a running summary above Deploy, so the assignments never get pushed to the bottom. Updated the flow map to match (V1 card / V2 modal / V3 wizard nodes; original Figma Create/Multi screens kept as reference, re-badged 'Figma')."
    },
    {
      "date": "2026-06-22",
      "path": "deployment/index.html",
      "summary": "Made the Flow Map version-aware: the shared designtoolbox/flow-map.js now supports per-node design versions (a version badge, version-pinned LIVE thumbnails via an fmv= hash token, and version-switching on Open live). Wired the deployment flow map to show all three versions — the existing Create/Multi flows badged V3 (Original Figma) plus new V1 (setup card) and V2 (setup modal) nodes in their own lanes — so the map reflects each design version and updates live as the designs change."
    },
    {
      "date": "2026-06-22",
      "path": "deployment/index.html",
      "summary": "Added a V3 to the Deploy Existing Assignments version switcher that shows the original Figma-imported Create New Deployment screen with the 'Create from scratch' / 'Select existing assignments' radio chooser (showVersion('3') drops into applyFlowState('n1'))."
    },
    {
      "date": "2026-06-22",
      "path": "deployment/index.html",
      "summary": "Made both V1 and V2 setups dates-first: Effective Dates is the only thing needed to pull and apply assignments, and Name / Work Types / Notes moved into a collapsed optional 'Deployment details' disclosure that can be filled in now or later (via Edit setup). Recap chips now show only what's filled in plus an 'Add name / work types later' reminder."
    },
    {
      "date": "2026-06-22",
      "path": "deployment/index.html",
      "summary": "Swapped the Deploy Existing Assignments design versions: V1 is now the inline setup card → 'Pull & Apply' (modeless) flow and V2 is the setup-modal-popup → full-screen picker flow (previously the reverse). Done by exchanging the two branches in showVersion() so button labels still match; default boot lands on V1 (the card flow)."
    },
    {
      "date": "2026-06-22",
      "path": "deployment/index.html",
      "summary": "Fixed the V2 'Deploy Existing Assignments' design so it no longer shows V1's setup popup. Switching from V1 (whose setup modal auto-opens) to V2 left the V1 modal open over the V2 view; showVersion() now always dismisses the V1 modal before rendering, so V2 only ever uses its inline setup card."
    },
    {
      "date": "2026-06-22",
      "path": "deployment/index.html",
      "summary": "Replaced the invented work-type options (Standard Shift / Training / On-Call) on the deployment work-type selects with the real CrewScheduler work types — Regular Time [REG], Comp Time [CT], Overtime [OT], Holiday [HOL] — matching the production fields and the naming used across the other Scheduling mocks. Feeds the main form's On-/Off-Duty selects and the V1/V2 setup selects."
    },
    {
      "date": "2026-06-22",
      "path": "Rules engine only/index.html",
      "summary": "In Preview Mode, the 'Add details — operational filters' drawer no longer pushes the preview ranking down when opened. It now floats as a purple dropdown layered on top of the ranking, anchored to its disclosure header (scrolls if it runs long). The full config-panel version is unchanged — only the compact preview-strip instance overlays."
    },
    {
      "date": "2026-06-18",
      "path": "deployment/index.html",
      "summary": "Added a DESIGN V1/V2 version switcher for the Deploy Existing Assignments flow. V1: dates + work types + notes in a setup modal, then a full-screen assignment picker. V2: setup as a card with a 'Pull & Apply' button that collapses it to a summary strip and expands the picker to full width. Both reuse the existing assignment cards + Select Employee picker."
    },
    {
      "date": "2026-06-18",
      "path": "deployment/index.html",
      "summary": "Moved Create New Deployment off the centered card to a full-width, two-column working layout: a full-width header band (title + deployment-type chooser side by side) over a grid of the form (left, in a bordered surface with two-up field rows) and a sticky live 'Deployment summary' rail (right). The rail recaps the deployment as it's built — color swatch, name/type, date range, location, on/off-duty work types, and a positions-filled progress bar — and updates live from the inputs, Flow A's qualifier cart, and Flow B's selected assignments + template. This fills the extra width with something useful so the screen no longer reads as empty."
    },
    {
      "date": "2026-06-18",
      "path": "deployment/index.html",
      "summary": "Reworked the Create New Deployment form so it no longer feels bare: it now sits in a centered white card on the neutral page (was full-bleed white with inputs stretched to 1320px), grouped into labeled sections (Deployment details, Schedule, Work types, Notes) separated by hairline dividers, with two-up field rows (Name+Code, the two Work Type selects). Rebuilt the inline Create-Template builder to use the same 'shop into a cart' interaction as Flow A's 'Select qualifiers required to fill positions' — pick a qualifier from the dropdown to drop a position in, adjust counts with steppers, add no-qualifier open positions, and watch a live position preview on the right (replaces the old add-chip + number-field approach). Also forced input fields inside gray panels (qualifier columns, template panel) to render white for contrast."
    },
    {
      "date": "2026-06-18",
      "path": "deployment/index.html",
      "summary": "Fixed the 'Select to deploy' flow on the Edit Assignments landing: the multi-select deploy bar was rendering as a tall dark column on the right because it sat as a flex sibling of the main content. It's now a floating bar fixed near the top, centered over the content area (offset past the sidenav), that fades/slides in when select mode is on — showing 'N selected', Cancel, and 'Deploy selected →'. Row checkboxes on deployable assignments are unchanged."
    },
    {
      "date": "2026-06-18",
      "path": "deployment/index.html",
      "summary": "Demo polish: the prototype now opens straight into the Create New Deployment screen (Add New Deployment) instead of the Edit Assignments landing — flow-map deep links (incl. the 'land' node) are unchanged; removed the default pre-selection on the Flow B assignment cards so none start checked; and the shared Design Toolbox dock (comment + flow map) now stays hidden and only pops up on hover near the bottom-center."
    },
    {
      "date": "2026-06-18",
      "path": "deployment/index.html",
      "summary": "Converted the Select Assignment(s) modal into an inline section of the Create New Deployment form (Flow B) — it now replaces the old 'Select Assignment(s)' button instead of opening an overlay. The search, deployable-assignment card grid, template area, and inline Create-Template panel render directly in the form; the live grid is the roster (no separate Save Roster step), and the section dims/blocks until start & end dates are set."
    },
    {
      "date": "2026-06-18",
      "path": "deployment/index.html",
      "summary": "Closed the gaps against the Deploy Existing Assignment(s) PRD (SCHED-22431): (AC-7.8/7.9) multi-select assignments to deploy together via a 'Select to deploy' checkbox mode + sticky 'Deploy N selected' bar → date-pick → Select-existing flow with all pre-selected; (AC-7.6) single deploy now lives in a per-row ⋯ ellipses menu (Deploy/Edit/Delete) instead of a ⚡ button; (AC-5.1/6.2/6.3/6.5/US-21/26/27) Manage Templates page gained Create New Template + per-template Edit/Delete via a Template Editor dialog and a position-count/qualifier summary on each card; (AC-5.5) the template builders now take a primary + optional shared qualifier per position; (AC-1.3) tooltip on the Is Deployable? header; (AC-3.1) alphabetical sort of the assignment selection list. Templates are now a shared TEMPLATES list used by both the inline flow builder and the manage page. DEV-NOTES updated."
    },
    {
      "date": "2026-06-17",
      "path": "deployment/index.html",
      "summary": "Built out the UX-review backlog for Deploy Existing Assignments: (1) Select Assignment(s) is now a near-full-screen ~85% working view (not a narrow modal) with a search box and a broad multi-column card grid; (2) the inline Create-Template builder now supports naming the template, adding qualified positions, and a count of open positions with no qualifier (minimum staffing / partial coverage); (3) new Manage Templates page (header button) listing deployable assignments + templates with a filter toggle and search, replacing the old multi-step flow; (4) Quick Deploy ⚡ action on deployable assignment rows that opens a date-pick dialog then drops into the Select-existing flow with that assignment pre-selected; and widened the Create New Deployment form. DEV-NOTES updated for all of these."
    },
    {
      "date": "2026-06-17",
      "path": "deployment/index.html",
      "summary": "Existing-assignment flow (Flow B): the Create New Assignment Template flow is no longer a separate 'Select Qualifiers' modal — the qualifier picker is now an inline panel under 'Assignment Template(s)' inside the Select Assignment(s) modal, revealed by the '+ Create New Assignment Template' trigger (Cancel/Create inline; trigger hides while creating or once a template exists). Also: open slots in the deployment result on the form are now clickable and open the Select Employee people picker (flowB) to fill the position in place."
    }
  ],
  "mocks": {
    "deployment": {
      "title": "Deployments",
      "description": "End-to-end Deployments prototype imported from Figma. Starts on the Edit Assignments landing (production chrome); 'Add New Deployment' opens the full-page Create New Deployment form, which branches into Flow A (Create Deployment — interactive qualifiers panel + open-slot Select Employee picker) or Flow B (Select Existing Assignments — an inline Select Assignment(s) section with a searchable deployable-assignment card grid, slot bars, and an inline Create Assignment Template panel). Includes a 'Flow Map' dev tool: a branching map of every screen with an Edit Assignments entry node, hover-preview, and click-to-open-live deep-linking into both flows. Also includes a DESIGN V1/V2/V3 version switcher (bottom-center pill) comparing one unified deploy model — a 'Create deployment / Select existing assignment' toggle, the same shared details, a build area that handles a single one or many (with template-shell creation in either mode) — across three dates presentations: V1 keeps the effective dates in an inline card that collapses, then the build, then the details; V2 collects dates in a popup modal (Edit dates reopens it), then build then details; V3 is a 1-2-3 step wizard (dates → assignments+template → details) where each step collapses to a one-line summary with a running summary above Deploy. The original Figma Create-from-scratch / Select-existing screens remain reachable from the flow map as reference.",
      "status": "in-progress"
    },
    "AI search engine dashboard": {
      "description": "Conversational AI search interface for the CallBack scheduling dashboard.",
      "status": "concept"
    },
    "Event Indicator UX-2474": {
      "description": "Rethinking how events are grouped and indicated on the scheduling calendar. Includes a 'Current UI' companion page (current-ui.html) reproducing the live production day view as the 'before' comparison.",
      "status": "in-progress",
      "ticket": "UX-2474",
      "extraLinks": [
        {
          "label": "Current UI",
          "file": "current-ui.html"
        }
      ]
    },
    "Rules engine only": {
      "description": "Standalone rules engine view for configuring scheduling automations and constraints.",
      "status": "in-progress",
      "ticket": "UX-2258",
      "ticketUrl": "https://lmsportal.atlassian.net/jira/software/c/projects/UX/boards/458?search=rules%20en&selectedIssue=UX-2258"
    },
    "test": {
      "description": "Vanilla HTML / Vector-components port of the Rules Engine CallBack prototype — same purple AI gradients and friendly chip palettes, no React.",
      "status": "in-progress"
    },
    "Theme Colors": {
      "description": "System settings page for customizing theme, brand colors, and nav appearance.",
      "status": "concept"
    }
  }
}