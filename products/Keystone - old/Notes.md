When a tenant is selected and the user goes back to 

There should be 

If the user enters a postal code, it should auto-populate the city and state

Remove map button, just let use select the entire product row
add save action for entire page at top




Thanks, now Lets rename the main tenants tab to Keystone Tenants and on that tab lets make the search bar narrower so that the "create new" button can sit next to it at the top right of the table. Then the whole table can shift up. Then lets make the column headers sortable. Let's also add the pagination control from the web components set referrenced in the Claude.md file


when you scroll to the end of the create new tenant modal the grid behind it starts to scroll as well, please prevent this from happening